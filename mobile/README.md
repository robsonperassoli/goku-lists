# Goku Lists (mobile)

Expo app for iOS, Android, and web. See the [repo README](../README.md) for setup, environment variables, and commands.

Use pnpm in this package (`pnpm install`, `pnpm start`, `pnpm lint`). See [AGENTS.md](./AGENTS.md) for sync-queue and tooling notes.
